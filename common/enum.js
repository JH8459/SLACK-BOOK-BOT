const YN_ENUM = {
  YES: 'Y',
  NO: 'N',
};

const ACTION_ID_ENUM = {
  RETURN_DATE: 'return-date',
  RENT: 'rent',
  MODAL: 'modal',
};

const FILTER_EVENT_TYPE = {
  VIEW_SUBMISSION: 'view_submission',
};

module.exports = {
  YN_ENUM,
  ACTION_ID_ENUM,
  FILTER_EVENT_TYPE,
};
